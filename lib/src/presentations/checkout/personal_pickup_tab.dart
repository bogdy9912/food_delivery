import 'package:flutter/material.dart';

class PersonalPickupTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('This feature is not implemented yet!'));
  }
}